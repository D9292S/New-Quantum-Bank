# This project now uses uv package manager with pyproject.toml for dependencies.
# Please use uv to install dependencies instead of pip:
#
# Install uv:
#   curl -LsSf https://astral.sh/uv/install.sh | sh
#   
# For Windows:
#   powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
#
# Install dependencies:
#   uv venv
#   uv pip install -e "."
#
# For development dependencies:
#   uv pip install -e ".[development]"
#
# The requirements below are kept for compatibility but may not be up-to-date.
# Please refer to pyproject.toml for the most current dependencies.

discord.py>=2.0.0
motor>=3.0.0
pymongo>=4.0.0
python-dotenv>=0.19.0
python-dateutil>=2.8.2
asyncio>=3.4.3
colorama>=0.4.4
orjson>=3.6.0
dnspython>=2.1.0
aiohttp>=3.8.0
cryptography>=36.0.0
PyNaCl>=1.5.0
typing-extensions>=4.0.0
pytz>=2021.3
